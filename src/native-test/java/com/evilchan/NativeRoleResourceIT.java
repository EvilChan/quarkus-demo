package com.evilchan;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRoleResourceIT extends RoleResourceTest {

    // Execute the same tests but in native mode.
}